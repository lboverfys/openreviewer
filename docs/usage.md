# OpenReviewer 使用流程

平台接收 GitHub PR，用固定提交的代码、关联代码与项目规则进行 AI 审查，再由有权限的成员处理结论。它服务一个团队的多个仓库。

## 每个入口负责什么

| 入口 | 什么时候使用 | 会发生什么 |
| --- | --- | --- |
| 审查控制台 | 看新 PR、失败原因、当前进度 | 接收任务、等待 CI、执行审查，打开具体问题和调用记录 |
| 审查依据 → 知识文档 | 业务规则、项目约定变化时 | 保存有来源、版本和仓库范围的文档，启用后供新审查按相关性选取 |
| 审查依据 → 代码检索 | 想知道 AI 看过哪些关联代码，或手动查代码时 | 检索固定提交的代码索引；任务通常自动建索引，无需每个 PR 手动点 |
| 模型配置 | 接模型、填价格、调整检索开关时 | 配置审查模型、Agent 分工、输入限制，以及百炼向量与精排 |
| 待办与用量 | 审查完成后跟进处理和费用时 | 管理待办、审批、用量、方案版本与诊断；不是另一个模型审查入口 |
| 团队管理 | 成员和仓库策略变化时 | 设置角色、仓库范围、预算、审批负责人和并发限制 |
| 效果评测 | 想核对准确性或比较模型方案时 | 收录已有真实审查、记录有效问题/误报/漏报；不会自动再跑模型 |

## 一次普通审查

1. 在模型配置中保存连接、模型名称和估算单价。连接测试会真实调用服务商；通过后启用。
2. 三路 Agent 分别检查安全、规范和逻辑。相同模型可继承公共连接和价格；需要独立模型时再覆盖。
3. 知识文档选择正确仓库，正文注明来源、日期和规则；启用后才供新任务选择。已有任务和固定方案保留原快照。
4. 提交 GitHub PR，平台自动接收。CI 就绪后准备代码索引和上下文，再发起模型请求。
5. 在任务详情查看本次实际模型、请求 ID、Token、耗时、知识引用和检索来源。历史结果展示其原模型，不随当前配置改名。
6. 核对候选问题，再决定批准、发布、驳回或重试；批准不会自动发布到 GitHub。

三路结果无需进一步模型去重时，程序完成汇总，页面显示“已完成程序汇总”。此时第四个模型没有请求、不产生第四路 Token，用实际适用节点计算完成进度。

## 两个检索开关

- “审查时自动补充关联代码”决定审查是否自动检索同一提交的上下文。
- “允许向量与精排模型调用”决定是否调用百炼向量/精排服务。关闭后仍可用关键词和代码关系检索，不会暂停 GPT 审查。

开关位于“模型配置 → 代码上下文”，保存后对下一次检索操作生效。首次未保存时兼容原部署默认值；页面保存后的值优先。它是运营开关，不冻结在历史方案中。已发出的 HTTP 不会因随后关闭开关而凭空撤销。

索引有新增向量数与请求次数上限。达到上限时保留基础索引并显示原因；需要时在限额内继续补齐，不会一次性无界生成全部向量。

## 知识资料来源

内置 NiuMa 项目资料来自本地 `D:\learning-continuing\niuma` 下的版本化文档与飞书原文。各文档保存固定 Git 提交或飞书链接，并限定仓库。

“补充项目资料”只添加缺少的文档，保留已有人工编辑、停用和归档状态。它不长期同步飞书；规则变化后需要阅读新版本并更新文档。原文中的凭据、测试账号和环境私密配置不放入知识库。

旧资料存在需要区分的变化：2026-09-07 的单账号单身份替代早期多角色说明；9.11 会议纪要及 9.13 开发说明将游戏资格、段位和服务报价交由管理员维护；6.3 旧响应示例中的 `code=0` 以最新五位业务码约定为准。公开需求旧稿存在前后付费描述冲突，本轮不将其设为确定审查规则。

## 价格和统计口径

OpenAI 参考价来源：https://developers.openai.com/api/docs/pricing ，核对日期 2026-09-13。`gpt-5.6-luna` 标准短上下文每百万 Token：输入 $0.20、输出 $1.20、缓存读取 $0.02、缓存写入 $0.25。

百炼参考价来源：https://help.aliyun.com/zh/model-studio/model-pricing ，北京地域 `qwen3.7-text-embedding`、`qwen3.7-text-rerank` 均为人民币 0.50 元/百万输入 Token。美元参考折算使用 Frankfurter 2026-09-11 的 USD/CNY=6.7082，向上取到六位小数为 $0.074536。汇率来源：https://api.frankfurter.dev/v1/2026-09-11?base=USD&symbols=CNY 。

这些是可修改的估算参考，不是中转站实际账单。长上下文、加速、地区和优惠可能改变价格。没有记录用量或历史报价时显示未知，不按当前价格改写旧费用。

## 如何开始评测

先收录一条已完成审查，填写已知参考缺陷，核对每条发现是否成立，并提交复核。样本管理会显示单组的复核进度、有效问题和误报，不需要先造一个候选组。

换模型、Prompt 或检索方案之后，针对同一 PR 和同一提交重新运行，再收录候选组。“对比报告”只在两个独立运行满足复核条件后计算比较指标。没有候选运行显示“等待候选运行配对”，不是把已做的复核清零。

AI 账号复核应明确标注，不能当作独立人工金标。小样本、模拟接口、录制输出与真实模型效果分别报告。当前整改阶段暂停 PR #93，等配置和流程验收完成再开始真实评测。
