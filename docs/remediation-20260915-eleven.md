# 十一项整改与本地验收，喵

本轮从 `main / 925f8e1` 开始。用户已授权修复后提交、推送并跟进现有 CI/CD；没有修改 NiuMa 业务源码，没有新增真实 PR，也没有发起线上收费模型测试，喵。

| 项目 | 改动与使用方式 | 本轮验证 |
| --- | --- | --- |
| 1. 检索失效 | 手动搜索自动从实际关键词、语义候选选择关系起点，按起点排名扩展调用与 SQL；参考答案只用于评分。新解析保留方法 Javadoc，旧索引继续保存。对比详情记录各路数量，按需读取完整检索过程；报告按所选仓库版本筛选，喵。 | 原 SHA、原查询、原三个 SQL 离线复测：关键词直接找回 0/3，关键词加关系找回 2/3；有关系候选 14 条。没有改变评分口径，喵。 |
| 2. Agent 失败 | 将代码/规则引用校验接入现有的一次自动纠错，失败原因按具体字段说明；始终拒绝未知代码，成功批次继续复用，喵。 | 只读确认任务 `809c436c-8a2c-4266-b152-0a0c47fbfe4f` 的规范第 2 批为 `location_file` 不一致。离线测试验证纠错成功、再次错误时停止、两次请求均计入用量；线上原任务未重跑，喵。 |
| 3. 即时提示 | 保存、操作错误等改为顶部通知，成功约 4.5 秒、错误约 7 秒后消失，也可手动关闭。业务状态仍留在报告内，喵。 | Chrome 保存本机预算后通知为 `position: fixed`；正文不被挤动。自动消失、关闭、切页清理有 DOM 回归，喵。 |
| 4. 页面下移 | 修复外壳把顶部导航当作剩余空间填充区域的旧 CSS；切换地址时恢复页面顶部，喵。 | 桌面导航高 68px；390px 窄屏导航约 71px，正文紧接导航，页面无横向溢出，喵。 |
| 5. 价格控件重叠 | 数字框与金额单位各占独立空间，保留原生增减能力，喵。 | Chrome 输入 `12345.678901` 与 `0.074536`，四个价格框与单位之间约 8px 间隔，无重叠，喵。 |
| 6. 入口不显眼 | 搜索代码、知识文档、检索模型与开关归入明确的按钮区域；相关操作沿用统一按钮样式，喵。 | Chrome 检查实际入口、文字与目标地址，喵。 |
| 7. 长详情下方展开 | 使用统一的大弹窗展示检索依据、历史返回代码、批次、规则引用、配置记录、变更代码等；正文在弹窗内部滚动，喵。 | Chrome 实际打开对比依据、检索过程及 SQL；弹窗宽 1120px，进入浏览器顶层，不挤压后面的正文，喵。 |
| 8. 粗糙表单 | 复选项改为对齐的可点击选项卡，去除生硬边框，统一间距、选中态和主要动作，喵。 | Chrome 确认复选框与文字同排，每项高度约 46.5px，喵。 |
| 9. 旧展开内容残留 | 切换地址会关闭详情和手册，卸载时释放滚动限制；关闭弹窗恢复焦点。设置分区保留既有草稿状态，喵。 | Chrome 关闭嵌套弹窗再切页，确认没有遗留弹窗，滚动限制已清理；DOM 回归覆盖嵌套关闭和切页，喵。 |
| 10. 跳转含糊 | 删除全局无仓库上下文的预算入口，在每个仓库用量行提供预算设置，直接打开该仓库的预算表单；评测和配置相关链接携带目标，喵。 | Chrome 直接打开 NiuMa 本机预算表单并保存；后台精确查询和前端深链接有回归，喵。 |
| 11. 评测复杂 | 开始新评测 → 选择一份审查 → 直接逐条点击有效/误报/不确定 → 完成并查看统计。名称可不填，单条记录不再绕进样本列表。另一份审查仅作为可选比较；显示提交、模型、配置版本和保存的来源，喵。 | Chrome 使用一个测试账号完成三条判断，结果为有效 1、误报 1、不确定 1、未核对 0；未知费用仍为未知。后台验证当前统计不混入另一份审查或其他 PR，喵。 |

## 验证范围，喵

- 本轮实现期间全量后端：689 通过、14 个 PostgreSQL 专用用例因本机未配置独立实例而跳过，覆盖率约 79%。最后涉及解析器升级、冻结证据、评测统计和引用纠错的相关回归：124 通过、1 个 PostgreSQL 专用用例跳过。最终提交的 PostgreSQL 与容器验收由现有 CI 执行，喵。
- 前端全量 253 项通过，语句覆盖率 57.36%，达到仓库门槛；最后的评测、通知、预算及原八项操作回归再检查 17 项通过，喵。
- Ruff、Mypy、TypeScript、OpenAPI 快照、真实评测契约快照、前端构建通过。24 个记录输出回放用例通过，仅属于管线回归，不是本轮真实模型准确率，喵。
- 浏览器仅使用 Chrome DevTools MCP。实际点击验收使用本机独立数据库，测试数据明确标记，不代表线上模型成绩。截图保存被 MCP 目录策略拒绝，无路径截图调用发生卡顿；因此本轮浏览器证据为实际操作、可访问性快照和布局尺寸检查，未声称截图验收通过，喵。

## 数据访问与兼容性，喵

- `code_relations`：按索引和有限起点一次 JOIN 取两跳关系，最多 500 行；复用现有索引与候选限制。单次关系检索查询次数 O(1)，喵。
- `retrieval_traces`、`retrieval_evaluations`：有界评测过程批量写入，详情按主键读取；列表复用游标分页，并使用 `index_id, created_at` 索引筛选。单页读取 O(1)，不在报告列表加载代码正文，喵。
- `evaluation_cases`、`evaluation_observations`：一个评测最多 200 个 PR、每个 PR 至多两份结果；权限核对加一次聚合，查询次数 O(1)，不读取完整快照，喵。
- `repository_policies`：预算跳转使用唯一 `repository_key` 索引，精确读取一条，不遍历仓库列表，喵。
- 新解析和旧索引分开保存，向量缓存命名空间保持兼容，相同文本无需再次生成向量。已有部分参考代码的任务继续使用同一旧索引，避免中途换证据；回归已覆盖此边界，喵。
- 无数据库迁移，无线上数据修补，无线上规则删除或账户权限变更，喵。

## 环境与产物，喵

- Python：`D:\rubbish\zhongjian\envs\openreviewer\dev\Scripts\python.exe`；Node：`D:\Code\nodejs\node.exe`，22.19.0；前端依赖复用项目 `web/node_modules`。没有安装依赖，喵。
- 临时数据库、测试日志、缓存、覆盖率与离线检索报告：`D:\rubbish\zhongjian\temp\openreviewer-20260915-eleven`，本轮检查约 594 MiB。最终真实源码离线复测报告为其中的 `original-query-v2.json`，喵。
- 构建：`D:\rubbish\zhongjian\artifacts\openreviewer\20260915-eleven\web-publish`；连同两轮本地验收构建约 2.5 MiB。所有可配置的测试和构建写入均在 D 盘；C 盘只使用已有工具标准配置与必要状态，喵。
- 原有未跟踪文件 `tests/performance/workspace_preview.py` 未修改、未删除、不提交，SHA256 保持 `718593281122B5E3A5AC1C136A62E4B7D80722545E07F8762C845685A3DFC182`，喵。

## 仍需如实说明的限制，喵

原始问题的订单行锁 SQL 尚未进入本次基础检索前 8 条，不能称为全部找回。真实语义和精排的新版效果未重新收费调用；它们的代码路径通过离线测试，但不能当作真实模型成绩。旧索引保留原解析，需要在网页选择同一提交建立新版基础索引后才能使用新增注释；新审查自动采用新解析，喵。

线上失败任务保留原始失败事实，没有为了验收触发计费重试。发布后可在该任务的“AI 检查过程”点击规范审查的重试按钮，使用新纠错逻辑继续。CI/CD 成败以最终推送提交对应的 Actions 记录为准，喵。
