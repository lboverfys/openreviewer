# 最终实施记录

依据工作区上级《OpenReviewer-最终实施方案.md》版本 1.0，代码基线 `6b61ad6`。
用户已授权分阶段中文提交、全部完成后推送 main、等待 CI/CD 并验证 niuma-2。
2026-09-21 补充范围：先完成代码和部署，真实评测稍后；本次不用浏览器 MCP。

| 工作包 | 功能与验证状态 | 真实证据状态 |
| --- | --- | --- |
| T1 评测口径、严格双人验收 | 已实现；后端 34 项、前端 6 项通过，2 项 PG 并发测试待 CI | 人工复核待后续 |
| T2 指定评测调用证据与归档 | 已实现；新增/关联 51 项通过，2 项 PG 待 CI；归档/迁移 23 项、前端相关 38 项通过 | 真实调用待后续 |
| T3 检索分层及确定性回归 | 已实现；本地检索 28 项、前端 8 项通过，隔离 PostgreSQL 16.15 正向与缺陷注入检查通过 | 人工数据集与收费策略对照待后续 |
| T4 Prompt 候选与同 SHA 试跑 | 已实现；后端阶段回归 32 项、补充 19 项通过，候选完整专项 5 项通过；前端相关 46 项通过 | 真实配对报告待后续 |
| T5 用量、健康度与复用口径 | 已实现；后端新增 4 项及原平台 14 项、前端相关 9 项通过；十万行 PG 计划待最终 CI | 真实收益对照待后续 |
| T6 关联日志、工程验收与告警 | 已实现；请求/线程/Worker/发布专项通过，隔离告警触发及恢复成功；全量检查进行中 | 外部渠道实际通知待授权接收方 |
| T7 架构、演示和证据归档 | 待实施 | 不填写未经测量的效果数字 |

本地复用 D 盘 Python 3.12.10 项目环境和 Node 22.19.0，无新增依赖。
测试与缓存位于 `D:\rubbish\zhongjian\temp\openreviewer\implementation-20260921`，
构建和归档分别位于 D 盘约定的 `build\openreviewer` 与 `artifacts\openreviewer`。
原有未跟踪文件 `tests/performance/workspace_preview.py` 保持不动且不纳入提交。

基线验证：评测/方案质量专项 14 项通过，1 项隔离 PostgreSQL 测试因未配置本地库跳过。
后续以最终提交 CI 的真实 PostgreSQL、故障恢复和规模测试作为工程验收依据。

T1：迁移 `20260921_0064` 给历史评测集保留 `single` 默认值；双人集按提交、一致性、
来源完整性筛选，参考/定位分歧分开统计。API 首次提交前隐藏他人判断，审计不泄露判断。
单人报告不能满足方案验收。稳定方案指纹与实际执行版本分开，批量收录仍为六条查询，
报告仍为单条 SQL。OpenAPI/生成类型已同步，相关 Ruff、Mypy、TypeScript 和迁移检查通过。

T2：迁移 `20260921_0065` 新增独立捕获表和运行开关/容量累计，默认不采集日常正文。
三种协议在解析前捕获可见答案，重试分别关联当前账本；单条 256 KiB、单运行 8 MiB，
30 天到期移除正文并保留过期元数据。新增受控导出、逐文件校验和报告复算入口。
没有使用真实模型，没有生成真实效果成绩。用量相关旧测试 107 项通过；其中一项新测试
曾错误匹配格式字符串，已改为检查字段和正文，随后通过专项回归。
同时修正 T1 的已绑定方案 Prompt 校验：按实际加载器计算内容版本后比对，不拿带内容后缀
的执行版本直接与裸协议版本比较。双人收录增加跨评测集的 PR 划分隔离。

T3：按 development/validation 复用逐项结果分桶，空分层指标为 null；前端默认显示验收集，
历史总体成绩只能在“全部样本”中查看。报告补充索引提交、解析器、候选数量、向量覆盖、
共享预热请求和各策略请求数量，不把无法完整还原的模型费用补为已知值。
新回归入口复用实际索引与 `HybridRetrievalService.evaluate()`；固定夹具明确标为合成契约数据。
2026-09-21 在 niuma-2 的临时 PostgreSQL 16.15 中实际测量并审阅冻结基线，R0 验证组找回
1/3、R1 找回 2/3；断开 relation_search 后门禁按预期失败。没有外部模型请求。
回归报告位于 `D:\rubbish\zhongjian\artifacts\openreviewer\implementation-20260921\retrieval`，
三个 JSON 实测合计 21553 字节；传输包 534614 字节。远端任务目录实测 3018138 字节，
已删除；临时容器已移除、25432 已无监听，原服务健康。CI 已加入同入口和报告归档。

T4：候选从基础不可变快照复制模型/参数/知识/检索，只修改角色指令和补充要求；固定协议
与 System Prompt 不开放修改。新版本分离协议与内容哈希，旧快照请求身份按原样兼容。
内容进入实际 HTTP 请求、来源与复用身份，已用内存传输测试验证，不请求真实模型。
历史试跑只替换新运行的方案绑定，当前仓库绑定、预算与并发策略保留；SHA、资源权限、
幂等参数、快照完整性和禁止发布在后端检查。相同运行混用程序版本不能作为严格来源。
该阶段保留 1 项本地未配置隔离 PG 的历史并发测试待最终 CI，相关静态与类型检查通过。

T5：Agent 费用与模型/用途共用窗口统计，前 100 组有截断标记，总计在限制前计算。
完成运行跨月归集，缺账本不算免费；预占偏差限定同批已结算且费用已知请求。
引用分类覆盖当前全部原因，未知不算模型错误；批次重复领取、HTTP 状态、组级缓存与
索引/结果复用分别统计。新增测试核对跨月、未知、范围权限、分位和 8 条固定查询。
测试初稿使用了数据库不允许的 NULL 状态/原因，已改为合法历史/未知样本并通过，
未修改数据库约束。十万条账本性能入口增加实际分组对账与 EXPLAIN ANALYZE，待 CI。

T6：统一 JSON 白名单日志与合法请求 ID 回传；提交/动作关联运行，Worker/Agent/批次请求
以及发布记录关联身份。线程池与心跳显式复制上下文，作用域结束恢复；不记录正文和异常原文。
前端错误可复制请求 ID，兼容原状态码、响应体和安全响应头。前端全量 264 项通过，覆盖率
行 64.03%、语句 58.28%、分支 54.17%、函数 48.82%；门槛提高为 60/55/50/45%，后端保留 75%。
全量前端检查发现旧单人测试样本缺新来源字段，补齐真实 API 已提供的字段，保留原断言。
隔离 Alertmanager v0.28.1 实际收到 firing/resolved；报告 `alert-result.json` 位于 D 盘任务
证据目录。线上真实挂载为 noop，未向任何外部人员或群组发送消息。专用容器、58327/44099
端口和远端临时目录已核实清理，生产 Alertmanager 仍运行。CI 新增规模执行计划产物归档。
